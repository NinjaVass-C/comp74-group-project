meta {
  name: /api/wallets/new
  type: http
  seq: 9
}

post {
  url: http://localhost:3000/api/wallets/new
  body: json
  auth: inherit
}

body:json {
  {
    "symbol": "AAPL"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
